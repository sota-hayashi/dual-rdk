# 方向空間価値学習モデル パラメータ推定 仕様書（MAP推定）

## 概要

各参加者の行動データから，von Mises基底関数を用いて連続円環方向空間上に定義された価値関数の学習パラメータ $(\alpha, \beta)$ をMAP推定により推定する．

## Q学習モデルとの違い

### 目的の違い

Q学習モデルは，分析者が定義した離散的な行動カテゴリ（ターゲット/ディストラクター）または刺激カテゴリ（白/黒）に対する報酬価値を更新し，その価値差が選択に反映されているかを検討するモデルであった．

本モデルは，参加者が知覚する連続的な方向空間上に価値関数を定義し，報酬経験によって特定の方向範囲への知覚的な重みづけが変化する過程を記述する．モデルの内部には白/黒やターゲット/ディストラクターといった離散的なカテゴリが存在せず，参加者の行動（報告角度）をそのまま連続量として扱う．

### 手段の違い

Q学習モデルでは，2つ（または4つ）のQ値を離散的に更新していた．本モデルでは，円環方向空間上に配置されたvon Mises基底関数の重みを更新することで，方向空間全体にわたる滑らかな価値関数を学習する．これにより，報酬情報が報告方向の近傍に局所的に般化し，高報酬方向の範囲全体の価値が徐々に上昇する過程を表現できる．

### 解釈の違い

Q学習モデルのsoftmaxは「どちらの行動（またはカテゴリ）を選択するか」の意思決定確率を表していた．本モデルのsoftmaxは「呈示された2つの方向のうち，どちらがより知覚的に顕著か」の競合結果を表す．参加者は常に「より強く知覚された方向を報告する」という行動規則に従っており，変化するのは方向空間上の知覚的な重みづけである．

---

## 入力

```python
concat_list: List[Tuple[str, pd.DataFrame]]
```

各タプルは `(subj_id, df)` の形式．DataFrameの使用カラムは以下の通り：

| カラム名 | 型 | 内容 |
|---|---|---|
| `rt` | float | 反応時間．NaNの試行は除外 |
| `response_angle_css` | float | 参加者が報告した方向角度（ラジアン，$[0, 2\pi)$） |
| `stimulus_angle_1` | float | 呈示されたドット群1の運動方向（ラジアン） |
| `stimulus_angle_2` | float | 呈示されたドット群2の運動方向（ラジアン） |
| `reward_points` | int | 0〜10の報酬値．$[0,1]$にスケーリング |

モデルの学習段階では，`stimulus_angle_1` と `stimulus_angle_2` はどちらがターゲットかの区別なく，単に「呈示された2つの方向」として扱う．元データに `target_direction`，`distractor_direction` として格納されている場合は，カラム名を変更するか，順序をランダムに入れ替えてモデルに渡す．

---

## 前処理

1. `rt` が NaN の試行を drop
2. `reward_points` を$[0,1]$ にスケーリング

---

## モデル定義

### von Mises基底関数

$N$ 個の基底関数を円環上に等間隔に配置する：

$$\mu_j = \frac{2\pi(j-1)}{N}, \quad j = 1, 2, \ldots, N$$

各基底関数は以下で定義される：

$$\phi_j(\theta) = \exp\left[\kappa \cos(\theta - \mu_j)\right]$$

$\kappa$ は集中度パラメータ（固定値として事前に設定）．

推奨設定：$N = 4$（基底中心：0°, 90°, 180°, 270°），$\kappa = 2.0$（報酬範囲の幅に応じて調整）．

### 価値関数

方向 $\theta$ に対する価値は基底関数の重みつき和で表現される：

$$V(\theta) = \sum_{j=1}^{N} W_j \cdot \phi_j(\theta)$$

### 重みの初期化

$$W_j(0) = 0, \quad j = 1, 2, \ldots, N$$

全方向に対して均一な価値（$V(\theta) = 0$）からスタートする．

### 重みの更新

各試行 $t$ で，参加者が方向 $\theta_{\text{reported}}$ を報告し，報酬 $r_t$ を受け取ったとき：

1. 報酬予測誤差を計算：

$$\delta_t = r_t - V(\theta_{\text{reported}})$$

2. 各基底関数の重みを更新：

$$W_{j,t+1} = W_{j,t} + \alpha \cdot \delta_t \cdot \phi_j(\theta_{\text{reported}})$$

$\phi_j(\theta_{\text{reported}})$ が掛かることにより，報告方向に中心が近い基底ほど大きく更新され，遠い基底はほとんど更新されない．

### 選択確率

各試行で呈示された2つの刺激方向 $\theta_A$, $\theta_B$ に対して：

$$P(\theta_A \text{を報告} \mid t) = \frac{e^{\beta V(\theta_A)}}{e^{\beta V(\theta_A)} + e^{\beta V(\theta_B)}}$$

この確率は「$\theta_A$ 方向が $\theta_B$ 方向より知覚的に顕著である確率」を表す．

---

## パラメータ

### 推定するパラメータ

| パラメータ | 範囲 | 意味 |
|---|---|---|
| $\alpha$ | $(0, 1)$ | 学習率：報酬予測誤差に対する重みの更新速度 |
| $\beta$ | $(0, \infty)$ | 逆温度：価値差が知覚的顕著性の差にどれだけ反映されるかの感度 |

### 固定するパラメータ

| パラメータ | 推奨値 | 意味 |
|---|---|---|
| $N$ | 4 | 基底関数の数 |
| $\kappa$ | 2.0 | 基底関数の集中度（般化の幅を決定） |

$\kappa$ を推定パラメータに含めることも可能だが，48試行での推定可能性を考慮し，まずは固定値として扱う．

---

## MAP推定

### 事前分布

| パラメータ | 事前分布 | 理由 |
|---|---|---|
| $\alpha$ | $\text{Beta}(2, 2)$ | $[0,1]$ に制約，中央付近に緩やかな山 |
| $\beta$ | $\text{Gamma}(2, 3)$ | 正値制約，過度に大きい値にペナルティ |

### 対数事後確率

$$\log P(\alpha, \beta \mid \text{data}) \propto \sum_t \log P(\theta_{\text{reported},t} \mid \theta_{A,t}, \theta_{B,t}, V_t) + \log P(\alpha) + \log P(\beta)$$

### 手順

1. $\alpha$ と $\beta$ の候補初期値をグリッドサーチで生成する
2. 各初期値 $(\alpha_0, \beta_0)$ に対して以下を実行する：
   a. 重み $W_j$ を全て0に初期化する
   b. 各試行 $t = 1, 2, \ldots, T$ について：
      - 呈示された2方向 $\theta_{1,t}$, $\theta_{2,t}$ に対して $V(\theta_{1,t})$, $V(\theta_{2,t})$ を計算する
      - softmaxで選択確率 $P(\theta_{1,t})$, $P(\theta_{2,t})$ を計算する
      - 参加者の報告角度 `response_angle_css` が $\theta_{1,t}$ と $\theta_{2,t}$ のどちらに角度的に近いかを判定し，近い方の選択確率の対数を対数尤度に加算する：$\mathcal{L} \mathrel{+}= \log P(\theta_{\text{closer},t})$
      - 報酬予測誤差 $\delta_t = r_t - V(\theta_{\text{reported},t})$ を計算する（$\theta_{\text{reported},t}$ は `response_angle_css`）
      - 重みを更新する：$W_{j,t+1} = W_{j,t} + \alpha \cdot \delta_t \cdot \phi_j(\theta_{\text{reported},t})$
   c. 対数事後確率を計算する：$\mathcal{L} + \log P(\alpha) + \log P(\beta)$
3. 負の対数事後確率を `scipy.optimize.minimize`（L-BFGS-B）で最小化する
4. 複数の初期値のうち，最小の負の対数事後確率を与えた $(\hat{\alpha}, \hat{\beta})$ を最適推定値とする

### 最適化の制約

- $\alpha \in (10^{-6}, 1 - 10^{-6})$
- $\beta \in (10^{-6}, \infty)$

---

## 出力

```python
results: pd.DataFrame
```

| カラム名 | 内容 |
|---|---|
| `subj_id` | 参加者ID |
| `alpha` | 推定された学習率 |
| `beta` | 推定された逆温度 |
| `log_posterior` | 最大化された対数事後確率 |
| `log_likelihood` | 対数尤度（事前分布なし） |
| `n_trials` | 使用試行数（NaN除外後） |
| `N` | 使用した基底関数の数 |
| `kappa` | 使用した集中度パラメータ |

---

## 事後分析：ターゲット選択確率の試行別推定

### 目的

推定されたパラメータを用いて，各試行におけるターゲット方向を報告する確率を復元する．モデルの学習段階ではターゲット/ディストラクターの区別を使用していないため，この事後分析でターゲット選択率の向上がモデルの帰結として生じるかを検証する．

### 追加入力

事後分析の段階で，各試行においてどちらの刺激方向がターゲットであるかの情報を使用する．これは元データの `target_direction`，`distractor_direction` カラムから取得する：

| カラム名 | 型 | 内容 |
|---|---|---|
| `target_direction` | float | ターゲット（高報酬側）ドット群の運動方向（ラジアン） |
| `distractor_direction` | float | ディストラクター（低報酬側）ドット群の運動方向（ラジアン） |

この情報はモデルの学習段階では使用されず，事後分析でのみ使用される．

### 手順

1. 推定された $\hat{\alpha}$, $\hat{\beta}$ を用いて，各参加者の試行データに沿って重み更新を再実行する
2. 各試行 $t$ における価値関数 $V(\theta, t)$ を復元する
3. 各試行で，ターゲット方向とディストラクター方向の価値を取得し，ターゲット選択確率を計算する：

$$P(\text{target}, t) = \frac{e^{\beta V(\theta_{\text{target},t})}}{e^{\beta V(\theta_{\text{target},t})} + e^{\beta V(\theta_{\text{distractor},t})}}$$

4. この軌跡を全参加者で平均し，試行を通じてターゲット選択確率が上昇するかを可視化する
5. 実データのターゲット選択率の前後半差分と，モデルから復元されたターゲット選択確率の前後半差分を比較する

### 出力

```python
trial_results: pd.DataFrame
```

| カラム名 | 内容 |
|---|---|
| `subj_id` | 参加者ID |
| `trial` | 試行番号 |
| `V_target` | ターゲット方向の価値 $V(\theta_{\text{target},t})$ |
| `V_distractor` | ディストラクター方向の価値 $V(\theta_{\text{distractor},t})$ |
| `p_target` | ターゲット選択確率 $P(\text{target}, t)$ |

---

## モデル評価

### 1. パラメータリカバリ

推定手続きが48試行・$N=4$ 基底のもとでパラメータを同定できるかを検証する．

#### 手順

1. $\alpha$ と $\beta$ の真値を複数水準で設定する（例：$\alpha \in \{0.1, 0.3, 0.5, 0.7, 0.9\}$，$\beta \in \{0.5, 1.0, 2.0, 5.0\}$の組み合わせ）
2. 各組み合わせに対して，実際の実験の刺激系列（各試行の $\theta_A, \theta_B$）を使用し，モデルに従って48試行分の人工行動データ（選択と報酬の系列）を確率的に生成する
3. 各乱数シードで複数回（例：100回）生成を繰り返す
4. 生成された各データセットに対して，仕様書記載のMAP推定手続きを適用し，$\hat{\alpha}, \hat{\beta}$ を推定する
5. 真値と推定値の対応を評価する

#### 定量的評価

| 指標 | 計算方法 | 判断基準 |
|---|---|---|
| Pearson相関 | 真値と推定値の相関係数 | 1に近いほど真値の大小関係を正しく復元できている |
| 平均絶対誤差（MAE） | $\frac{1}{M}\sum\lvert\alpha_{\text{true}} - \hat{\alpha}\rvert$ | 小さいほど推定精度が高い |
| バイアス | $\frac{1}{M}\sum(\hat{\alpha} - \alpha_{\text{true}})$ | 0に近いほど系統的偏りがない |

#### 定性的評価

- 真値（横軸）× 推定値（縦軸）の散布図を $\alpha$, $\beta$ それぞれについて作成し，対角線上にデータ点が乗るかを目視確認する
- 真値の各水準での推定値の分布をボックスプロットで表示し，推定が安定する範囲と崩れる範囲を特定する

#### 判断基準

- 相関が0.7以上，かつ散布図で対角線に沿う傾向が確認されれば，推定手続きは実用的に信頼できると判断する
- 相関が0.5未満，または特定のパラメータ範囲で推定値が一定値に張り付く場合は，そのパラメータの同定は困難と判断する

### 2. 事後予測チェック

推定されたモデルが実データの行動パターンを再現できるかを検証する．

#### 手順

1. 推定された $\hat{\alpha}, \hat{\beta}$ と実際の刺激系列を使って，モデルのシミュレーションを多数回（例：1000回）繰り返し，人工行動データを生成する
2. 生成データから試行ごとのターゲット選択率を計算し，実データのターゲット選択率と比較する

#### 定性的評価

- 実データのターゲット選択率の試行推移と，モデル生成データのターゲット選択率の試行推移（平均 ± 95%信用区間）を重ねてプロットする
- 実データの軌跡がモデル生成データの信用区間内に収まっているかを目視確認する

#### 定量的評価

| 指標 | 計算方法 | 判断基準 |
|---|---|---|
| ターゲット選択率の前後半差分 | 実データとモデル生成データの前後半差分を比較 | モデル生成データの95%区間が実データの差分を含むか |
| 試行別予測精度 | 各試行でモデルが予測したP(target)と実際の選択（0/1）の一致度 | AUCが0.5より有意に大きいか |

### 3. Q学習モデルとの比較

| 指標 | 計算方法 | 判断基準 |
|---|---|---|
| BIC | $-2 \mathcal{L} + k \log T$ | 低い方が良い（$k$: パラメータ数，$T$: 試行数） |
| AIC | $-2 \mathcal{L} + 2k$ | 低い方が良い |

両モデルのパラメータ数は同じ（$\alpha, \beta$ の2つ）なので，BIC/AICの差は純粋に対数尤度の差を反映する．
